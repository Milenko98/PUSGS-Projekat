import { WorkRequestEquipments } from './work-request-equipments';

describe('WorkRequestEquipments', () => {
  it('should create an instance', () => {
    expect(new WorkRequestEquipments()).toBeTruthy();
  });
});
