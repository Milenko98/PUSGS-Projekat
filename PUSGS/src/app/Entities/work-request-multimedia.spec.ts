import { WorkRequestMultimedia } from './work-request-multimedia';

describe('WorkRequestMultimedia', () => {
  it('should create an instance', () => {
    expect(new WorkRequestMultimedia()).toBeTruthy();
  });
});
